# Tests: disable Flyway to avoid failed migration blocking test runs
spring.flyway.enabled=false
# Allow Hibernate to update schema for test runs (adds created_by/updated_by if needed)
spring.jpa.hibernate.ddl-auto=update
